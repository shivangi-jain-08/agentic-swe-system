"""Top-level package for the Agentic SWE System.

This project intentionally uses `src` as the import root so the demo commands can run as:

  python -m src.graph --task "..." --sop sop/v1.0.yaml
"""
